desc "Build into the deploy repo"
task :build do
  system "cp -R features/descriptions/* ../patchspace_katas/pocket_calculator/spec"
  system "cp features/README.markdown ../patchspace_katas/pocket_calculator"
end